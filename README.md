# Gemini Streamlit Chat App ✨

This is a simple, modern chat application built using **Streamlit** and the **Google Gemini API**.

It allows users to interact with the powerful `gemini-2.5-flash` model directly through a clean web interface.

## 🚀 Features

* **Real-time Chat:** Conversational memory is maintained using the `client.chats` service.
* **Secure Secrets Management:** Uses Streamlit's built-in `st.secrets` to securely manage the Gemini API key.
* **Easy Deployment:** Designed for quick deployment on the Streamlit Community Cloud or any Python environment.

## ⚙️ Setup and Installation

### 1. Clone the Repository

```bash
git clone [YOUR_REPO_URL]
cd [YOUR_REPO_NAME]
